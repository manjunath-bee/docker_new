apiVersion: v1
kind: ConfigMap
metadata:
  name: myconfigmap
data:
  DB_HOST="mysql"
 --- 
apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: expense
  name: backend
  labels:
    app: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: manjudbadevops/backend:1.0.0
        envFrom:
        - configMapRef:
            name: myconfigmap
---
apiVersion: v1
kind: Service
metadata:
  namespace: expense
  name: backend
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080