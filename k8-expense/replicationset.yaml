apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: nginx
  namespace: expense
  labels:
    app: nginx
    tier: web
spec:
  # modify replicas according to your case
  replicas: 3
  selector:
    matchLabels:
      app: nginx
      tier: web
  template:
    metadata:
      labels:
        app: nginx
        tier: web
    spec:
      containers:
      - name: nginx
        image: nginx